#include "main.h"

/**
 * mul - Multiplies two integars
 * @a: first operand
 * @b: second operand
 *
 * Return: Result of operation
 */

int mul(int a, int b)
{
	int result = a * b;

	if ((a && b) == 1)
		return (result);

	return (0);
}
